node_modules/
dist/

# TypeScript incremental build cache: machine-local build output, regenerated by tsc.
# Tracked by accident 2026-07-02; a dirty copy makes `git rebase` REFUSE TO START, which
# blocked web_push every night after a frontend build (2026-08-08 Phase E stop).
*.tsbuildinfo
*.local
.vercel
.DS_Store

.vercel

# Option B: bulk data served via jsDelivr from bradleygpt/akribeia-data (do NOT track).
# These are produced locally by the bake and published to the data repo; the frontend
# resolves them via the freshness_manifest.json `blob` section. See ops/publish_data_repo.py.
public/data/detail_timeseries/
public/data/detail/
public/data/prices/
public/data/earnings_reviews.json
public/data/universe_floor0.json
public/data/universe_floor1.json
public/data/universe_floor10.json
public/data/mlpred.json
public/data/quarterly.json
public/data/correlations_cache.json
public/data/fcf_distortion.json
public/data/earnings_quality.json
